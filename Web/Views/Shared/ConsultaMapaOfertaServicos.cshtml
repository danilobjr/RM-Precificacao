@using RM.Precificacao.Dominio.Enumeradores;
@using RM.Precificacao.Web.Extensions;

<div id="consultaMapaOfertaServico" class="popup">

    <div class="filtros fieldset">

        <h2>Filtros</h2>
        
        <div class="content">

            @using (Html.BeginForm())
            {
                <div class="campo">
                    <label>Empresa</label>
                    <br />

                    @foreach (Empresa valor in Enum.GetValues(typeof(Empresa)))
                    {
                        var elementId = String.Format("consulta_{0}", @valor.GetStringValue()).Replace(" ", "_");
                        
                        <div class="campoRadio">
                            <input type="radio" id="@elementId" name="Empresa" value="@((int)valor)" />
                            <label for="@elementId">@valor.GetStringValue()</label>
                        </div>
                    }
                </div>

                <br clear="all" />
                
                <div class="campo">
                    <label>Tipo</label>
                    <br />

                    @foreach (TipoServico valor in Enum.GetValues(typeof(TipoServico)))
                    {
                        var elementId = String.Format("consulta_{0}", @valor.GetStringValue()).Replace(" ", "_");
                        
                        <div class="campoRadio">
                            <input type="radio" id="@elementId" name="TipoServico" value="@((int)valor)" />
                            <label for="@elementId">@valor.GetStringValue()</label>
                        </div>
                    }
                </div>

                <br clear="all" />
                
                <div class="campo">
                    <label>Segmento</label>
                    <br />
                    <select name="IdSegmento">
                    </select>
                </div>

                <div class="campo">
                    <label>Serviço</label>
                    <br />
                    <input type="text" name="DescricaoServico" />
                </div>

                <div class="botoes">
                    <button>Filtrar</button>
                </div>
            }

        </div>
    </div>

    <section>
        <table id="tabelaConsultaMapaOfertaServicos">
            <thead>
                <tr>
                    <th>Empresa</th>
                    <th>Tipo</th>
                    <th>Segmento</th>
                    <th>Serviço</th>
                    <th>Referência</th>
                </tr>
            </thead>

            <tbody>
            </tbody>
        </table>
    </section>

</div>